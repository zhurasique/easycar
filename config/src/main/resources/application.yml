spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/shared
  security:
    user:
      password: ${EASYCAR_PASSWORD}
  profiles:
    active: native

server:
  port: 8888

